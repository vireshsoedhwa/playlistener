version: '3.8'

services:
  app:
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    environment:
      - DEBUG=1
      - PRODUCTION=0
    ports:
      - "8000:8000"
    networks:
      playlistener-network:    
        aliases:
          - playlistenerapidev